name: Pwnage
categories:
  - binex
value: 50
flag:
  file: ./flag.txt
description: |-
  It's either a bug, a hack, an exploit, or it's pwnage.

  Let this challenge stand as one of the first of many stairs to mastery
  over that which can only be described as pwn.
hints:
  - Connect using `nc` aka Netcat
authors:
  - awt
visible: true
deploy:
  nc:
    build: .
    expose: 9999/tcp
